apiVersion: v1
kind: Service
metadata:
  name: {{ include "erpadsapi-subs.fullname" . }}
  labels:
    {{- include "erpadsapi-subs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "erpadsapi-subs.selectorLabels" . | nindent 4 }}
