apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "erpadsapi-subs.fullname" . }}-test-connection"
  labels:
    {{- include "erpadsapi-subs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "erpadsapi-subs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
