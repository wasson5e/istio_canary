apiVersion: v1
kind: Service
metadata:
  name: {{ include "istio-canary.fullname" . }}
  labels:
    {{- include "istio-canary.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "istio-canary.selectorLabels" . | nindent 4 }}
