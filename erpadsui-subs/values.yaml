# Default values for erpadsui-subs.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

team: "ERP"
supportEmail: "DLCorpIT-ERP@charter.com"

replicaCount: 1

image:
  repository: docker.corp.chartercom.com/erp/nginx_ldap
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: ""

imagePullSecrets: [{ name: "regsecret" }]
nameOverride: "erpadsui-subs"
fullnameOverride: ""
namespace: erpadsui

virtualService:
  hosts: "adssubs<ENVIRON>.corp.chartercom.com"
  port: 80
  name: erpadsui-subs

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: 
  capabilities:
    drop:
    - ALL
    add:
    - NET_BIND_SERVICE
  runAsNonRoot: true
  runAsUser: USERID
  allowPrivilegeEscalation: false
  seccompProfile:
    type: RuntimeDefault

service:
  type: ClusterIP
  port: 80
  targetPort: https

ingress:
  enabled: true
  className: istio
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  tls:
    - hosts:
        - adssubs<ENVIRON>.corp.chartercom.com
      secretName: erpadsui-tls-secret
  hosts:
    - host: adssubs<ENVIRON>.corp.chartercom.com
      paths:
        - path: /
          pathType: ImplementationSpecific
          backend:
            service:
              name: erpadsui-subs-service
              port:
                number: 80

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
    cpu: 200m
    memory: 1024Mi
  requests:
    cpu: 100m
    memory: 500Mi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

# Additional volumes on the output Deployment definition.
volumes: {}

# Additional volumeMounts on the output Deployment definition.
volumeMounts: {}

nodeSelector: {}

tolerations: []

affinity: {}
