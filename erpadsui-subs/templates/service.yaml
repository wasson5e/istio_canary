apiVersion: v1
kind: Service
metadata:
  name: {{ include "erpadsui-subs.fullname" . }}
  labels:
    {{- include "erpadsui-subs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "erpadsui-subs.selectorLabels" . | nindent 4 }}
