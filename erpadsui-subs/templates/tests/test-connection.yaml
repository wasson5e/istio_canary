apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "erpadsui-subs.fullname" . }}-test-connection"
  labels:
    {{- include "erpadsui-subs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "erpadsui-subs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
